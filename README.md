# Музыкальная картотека

PWA-картотека любимой музыки: владелец ведёт её на телефоне, друзья смотрят по ссылке.
Полная документация — `docs/Музыкальная картотека — документация для разработки.docx`.

## Статус

| Этап | Что | Статус |
| --- | --- | --- |
| 0 | Разведка: `app/public/probe.html`, ADR 0001, 0003, 0004 | iPhone проверен; данные и обложки — iTunes |
| 1 | Каркас: Vite + TS + Preact, PWA, слой данных, CI/CD | готово |
| 2 | Картотека: создание, редактирование, удаление, треки, теги, ссылки, обложки | готово |
| 3 | Главная и подбор: поиск, чипы тегов, сортировки, фильтры в адресе, «Удиви меня» | готово |
| 4 | Автозаполнение из iTunes: поиск, треклист, обложка, цвета | готово |
| 5 | Публикация на GitHub, автопубликация, режим зрителя, конфликты | готово |
| 6 | Резервные копии: экспорт/импорт ZIP, импорт с сайта | готово |
| 7 | Полировка: визуал «Перламутр и стекло», диск, переходы, «Удиви меня» — проигрыватель | в работе |
| 8 | Запуск | впереди |

## Запуск

```bash
cd app
npm install
npm run dev          # http://localhost:5173
npm test             # модульные и интеграционные тесты (Vitest + fake-indexeddb)
npm run build        # сборка в app/dist
npx playwright install chromium webkit   # один раз
npm run e2e          # e2e-сценарии на мобильных профилях
```

Чтобы открыть dev-сервер с телефона в той же Wi-Fi-сети: `npm run dev -- --host`.
Для проверки PWA и офлайна нужна продакшен-сборка: `npm run build && npm run preview -- --host`.

## Структура

```
app/            приложение (собирается GitHub Actions)
  public/probe.html   страница этапа 0 — проверка внешних API с телефона
  src/data/           модель, Repository, IndexedDB (localSource), catalog.json (remoteSource), миграции
  src/services/       обложки (сжатие, палитра), ссылки, поиск и фильтры (search.ts), iTunes (itunes.ts),
                      Spotify (spotify.ts, spotifyAuth.ts), резервные копии (backup.ts)
  src/screens/        Home, AddSearch, Release, Editor, Settings
  src/components/     Cover, TagChip, TagInput, TrackEditor, LinksEditor, диалоги
data/           опубликованные данные картотеки (пишет приложение, этап 5)
docs/adr/       архитектурные решения
.github/workflows/  ci.yml, deploy.yml
```

## Публикация и ссылка для друзей

1. Создать fine-grained token: GitHub → Settings → Developer settings → Fine-grained tokens,
   доступ только к `music-catalog`, Repository permissions → Contents: Read and write, срок 1 год.
2. В приложении: Настройки → Публикация → репозиторий и токен → «Подключить и опубликовать».
   Токен хранится только на этом устройстве.
3. Дальше картотека публикуется сама через 5 секунд после каждого изменения; сайт обновляется примерно через минуту.
4. Друзья открывают ту же ссылку (`https://<логин>.github.io/music-catalog/`) и видят картотеку только для просмотра.
5. Новый телефон владельца: открыть ссылку → Настройки → «Это моя картотека» → токен.

Страница проверки внешних сервисов (этап 0): `https://<логин>.github.io/music-catalog/probe.html`.

Свой домен: файл `CNAME` в корне репозитория с именем домена — deploy.yml скопирует его на сайт.
